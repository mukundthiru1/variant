/**
 * VARIANT — Packages barrel export.
 */
export type {
    PackageManagerType,
    PackageDefinition,
    PackageFile,
    PackageMirrorConfig,
    PackageMirrorCommands,
} from './types';
export { createPackageMirror } from './types';
