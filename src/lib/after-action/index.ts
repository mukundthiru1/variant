/**
 * VARIANT — After-Action Report barrel export
 */
export type {
    AfterActionReport,
    SessionSummary,
    ScoreBreakdown,
    TimelineEntry,
    TimelineEntryType,
    Observation,
    Tip,
    MissedOpportunity,
    SkillAssessment,
    SkillLevel,
    Grade,
    AfterActionGenerator,
    AfterActionConfig,
} from './types';

export { createAfterActionGenerator } from './after-action-generator';
