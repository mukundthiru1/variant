/**
 * VARIANT — Vulnerable Web Application Module
 *
 * A configurable, realistic vulnerable web application for
 * security training scenarios.
 */

export { createVulnerableWebApp } from './vulnerable-app';
export type { VulnerableWebAppConfig, VulnerabilityId, User, SessionData } from './vulnerable-app';
